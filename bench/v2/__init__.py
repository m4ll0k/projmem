"""v2 benchmark harness — see bench/v2/README.md.

Each setup is a self-contained Python module under its own subdir
(reintroduction/, guidance/, critical/). The harness is operator-run,
not CI-run — real LLM API calls cost real money.
"""
