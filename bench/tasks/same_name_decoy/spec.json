{
  "id": "same_name_decoy",
  "kind": "same-name-ambiguity",
  "family": "wrong-file-ambiguity",
  "tier": "hard",
  "description": "Three modules define compute_rate(); only the billing one is authoritative and needs the 0.15 rate bug fixed.",
  "prompt": "There is a bug in compute_rate(): the production billing path returns 0.15 when it should return 0.25 for customers tagged 'enterprise'. Fix the billing path ONLY. The admin/ and vendor/ modules also define a function named compute_rate(); DO NOT modify them — they serve different domains. Update the billing path and adjust tests/test_rates.py so the enterprise case is verified.",
  "required_files": [
    "app/billing/rates.py",
    "tests/test_rates.py"
  ],
  "decoy_files": [
    "app/admin/rates.py",
    "app/vendor/rates.py"
  ],
  "regex_checks": [
    {
      "file": "app/billing/rates.py",
      "pattern": "0\\.25",
      "min_matches": 1,
      "reason": "enterprise path must return the correct rate"
    },
    {
      "file": "app/billing/rates.py",
      "pattern": "0\\.15",
      "max_matches": 0,
      "reason": "the buggy 0.15 literal must be gone from billing"
    },
    {
      "file": "tests/test_rates.py",
      "pattern": "enterprise",
      "min_matches": 1,
      "reason": "test must cover the enterprise path"
    }
  ],
  "test_command": "python3 -m pytest tests/ -q",
  "must_not_claim": [
    "updated all compute_rate definitions",
    "touched admin and vendor for parity"
  ]
}
