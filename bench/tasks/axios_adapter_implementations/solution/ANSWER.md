# Axios HTTP transport adapter implementations

The three concrete adapter implementations under `lib/adapters/` are
listed below. The registry (`adapters.js`) and the documentation file
(`README.md`) are intentionally excluded — they are not transport
implementations.

adapter: lib/adapters/fetch.js
adapter: lib/adapters/http.js
adapter: lib/adapters/xhr.js
